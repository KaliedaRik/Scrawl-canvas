# Scrawl-canvas artefacts and the DOM
Artefacts are SC objects that wrap DOM elements
+ Canvas artefact
+ Stack artefact
+ Element artefact

Manipulating the DOM during SC initialization
+ Stack - element and canvas positioning
+ Canvas - making the canvas accessible

Interplay between CSS and SC
+ CSS `position`
+ 3D rotation

Other stuff
+ Groups are not layers
+ Intersection observers
+ SC snippets (experimental)