# Scrawl-canvas events and signals
Discuss how SC handles events
+ The pub-sub signalling system and "dirty" flags
+ User preferences and system events
+ Browser resize events and responsive behaviour - including Canvas/Stack shape and size
+ Browser scroll events and Canvas/Stack viewport position
+ Keyboard events
+ Mouse, touch and pointer events
+ Drag-and-drop behaviour
+ Bespoke event listeners: `scrawl.addListener()`, `scrawl.addNativeListener()`
